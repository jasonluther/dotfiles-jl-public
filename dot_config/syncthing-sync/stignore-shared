// Shared Syncthing ignore patterns applied to every folder by apply-ignores.sh.
// Keep this list generic — no project, path, or host-specific entries.
// Per-folder overrides live in ~/.config/syncthing-sync/overrides.json (not versioned).

// macOS
.DS_Store
**/.DS_Store
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
.fseventsd
.TemporaryItems
.DocumentRevisions-V100
Icon?

// Editors / IDEs
*.swp
*.swo
*~
.idea
.vscode/.history

// Build / dependency directories
// Only patterns that are unambiguously tool-specific go here. Common English
// words like `build`, `dist`, `target` are intentionally NOT included — they
// can collide with user data directories named the same thing (e.g. a CNC
// project's `build/` containing photos). Add those as per-folder overrides
// in ~/.config/syncthing-sync/overrides.json for code-only folders.
node_modules
.venv
__pycache__
*.pyc
.next
.turbo
.cache
.pytest_cache
.ruff_cache
.mypy_cache

// VCS junk
//
// Goal: replicate enough of .git/ that branches/refs/objects flow between
// machines, while suppressing the in-flight files that cause sync-conflict
// noise and (worse) stranded loose objects.
//
// Fully synced: refs, packed objects, completed loose objects.
// Excluded:    the machine-local index, reflogs, in-flight scratch, lock
//              files, atomic-rename temp files, hooks, config, info/exclude,
//              OS metadata.
//
// .git/index is a per-machine cache of working-tree stat data (inode/mtime/
// size). It is rewritten by almost every git command and is meaningless on
// another host, so syncing it only produces conflicts; git rebuilds it
// locally on the next command after refs/objects arrive.
//
// Reflogs (.git/logs) and the scratch files (FETCH_HEAD/ORIG_HEAD/
// COMMIT_EDITMSG/MERGE_*) are per-operation churn, not needed for branches
// or objects to flow between machines.
//
// This does NOT prevent legitimate conflicts when both machines write the
// same ref (e.g. both committing on `main` independently). For that you
// need single-writer discipline; see resolve-git-sync-conflicts for repair.
//
// hooks/, config, and info/exclude are machine-local, not history: git's
// default hooks/*.sample are byte-identical everywhere (pure conflict noise),
// installed hooks belong to the local checkout, config carries per-host remote
// and core.* settings, and info/exclude is a per-machine ignore list. None
// need to flow between machines (a fresh checkout gets them from `git clone`),
// and syncing them only manufactures conflicts. On 2026-07-07 these produced
// the bulk of a 500-file sync-conflict pileup.
.git/index
.git/logs
.git/hooks
.git/config
.git/info/exclude
.git/FETCH_HEAD
.git/ORIG_HEAD
.git/COMMIT_EDITMSG
.git/MERGE_HEAD
.git/MERGE_MSG
.git/*.lock
.git/**/*.lock
.git/objects/*/tmp_*
.git/objects/pack/tmp_*
