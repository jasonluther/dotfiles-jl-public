# Cross-platform CLI tools — brew on macOS, apt on Debian.
# Format: one package name per line. Blank lines and `#` comments allowed.
# Use the brew formula name; for differing apt names add an entry in apt-aliases.txt.

awscli
bat
clang-format
cmake
direnv
dos2unix
eza
ffmpeg
fswatch
gh
git
git-delta
git-filter-repo
jq
lazygit
markdownlint-cli
miller
nmap
node
pandoc
pipx
prettier
ripgrep
shfmt
tmux
tree
uv
vale
yq
zoxide

# Best-effort on Linux — older Debian/Ubuntu lack a watchexec apt package.
# install-packages.sh will report it as skipped; install via cargo or the
# upstream .deb release if you need it.
watchexec

# Best-effort on Linux — kondo isn't in apt. Install via `cargo install kondo`
# or grab a release from https://github.com/tbillington/kondo.
kondo
