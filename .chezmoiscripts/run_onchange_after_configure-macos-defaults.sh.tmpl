#!/usr/bin/env bash
# Thin wrapper for scripts/configure-macos-defaults.sh.
# run_onchange_: re-runs whenever the wrapped script's content changes.
#
# script hash: {{ include "scripts/configure-macos-defaults.sh" | sha256sum }}

exec {{ joinPath .chezmoi.sourceDir "scripts/configure-macos-defaults.sh" | quote }}
