#!/usr/bin/env bash
# Thin wrapper for scripts/linux/setup-google-chrome.sh.
#
# Runs before install-packages so apt knows about Google's repo by the
# time the package installer fires. The `00-` prefix forces this ahead
# of install-packages in lexicographic order.
#
# script hash: {{ include "scripts/linux/setup-google-chrome.sh" | sha256sum }}

{{ if eq .chezmoi.os "linux" -}}
exec {{ joinPath .chezmoi.sourceDir "scripts/linux/setup-google-chrome.sh" | quote }}
{{- else -}}
# Linux-only: skipped on {{ .chezmoi.os }}.
{{- end }}
